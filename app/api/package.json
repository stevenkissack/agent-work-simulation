{
  "name": "worktasker-api",
  "version": "0.2.0",
  "private": true,
  "description": "Work Tasker backend: sites and tasks with due dates.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
